#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/.."

echo "==> Installing dependencies with pnpm..."
pnpm install

echo "==> Building TypeScript..."
pnpm build

echo "==> Bootstrap complete."
